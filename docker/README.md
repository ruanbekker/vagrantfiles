## Docker with Vagrant

```
$ vagrant up --provision
$ vagrant ssh
$ docker ps
CONTAINER ID        IMAGE               COMMAND             CREATED             STATUS              PORTS               NAMES
```
